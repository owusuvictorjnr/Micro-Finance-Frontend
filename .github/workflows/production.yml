name: Deploy Production (placeholder)

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          echo "Production deployment placeholder: no deployment steps are configured yet"
    